import { createStyles } from '@mantine/core';

export const useStyles = createStyles((theme) => ({
  boxContainer: {
    marginTop: '2rem',
    marginLeft: '2rem'
  },
  title: {
    fontSize: 30,
    fontWeight: 'initial',
    marginBottom: '2rem'
  }
}));
